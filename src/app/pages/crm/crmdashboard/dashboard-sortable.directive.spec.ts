import { DasbaordSortableDirective } from './dashboard-sortable.directive';

describe('DasbaordSortableDirective', () => {
  it('should create an instance', () => {
    const directive = new DasbaordSortableDirective();
    expect(directive).toBeTruthy();
  });
});
