export interface AppConfig {
  apiUrl: string;
  production: string;
  baseUrl: string;
  websocketBaseUrl: string;
  webbaseUrl: string;
  providerbaseUrl: string;
  chatbaseUrl: string;
  socketIOBaseUrl: string;
}
